# Spec: <short feature title>

<!--
Copy this file into specs/pending/ and name it with the next unused three-digit
global sequence and a kebab-case slug, e.g. 053-add-game-to-library.md. Check
both specs/pending/ and specs/done/ before choosing the number. Preserve it
when archiving; reference specs by slug, not number. Creation date goes in the
Created: field below, not the filename.

A spec describes WHAT and WHY, not HOW. Implementation details (files,
functions, libraries) belong in the plan/task phase, not the spec.
-->

Status: draft <!-- draft | ready | in-progress | review | done -->
Created: YYYY-MM-DD
Completed: <!-- date when moved to specs/done/ -->

## Problem
What user problem or pain point does this solve? Describe the current state
and why it is unsatisfactory. Be concrete.

## Goal
The desired outcome in one or two sentences. What should be true after this
ships?

## Users & Context
Who uses this, and in what situation?

## Requirements
Numbered behavioral requirements. Each must be independently verifiable: an
implementer and verifier can both determine whether it passes.

1. MUST ...
2. MUST ...
3. SHOULD ...

## Non-Goals
Explicitly list what is out of scope to prevent scope creep.

- We will NOT ...

## Scenarios (Acceptance Criteria)
Given/When/Then style. These double as test cases.

- **Given** ..., **When** ..., **Then** ...

## Open Questions
Unresolved decisions that block implementation. Resolve these before moving
the spec to `ready`.

- [ ] ...

<!-- Once ready, replace the checklist with "None outstanding." -->

## Resolved Decisions
Optional. Record only consequential defaults or tradeoffs: visible,
costly, security-sensitive, or hard-to-reverse choices. Do not log routine
technical decisions.

## Out-of-Scope Notes
Optional ideas to revisit later, but not for this iteration.
