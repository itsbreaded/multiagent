interface:
  display_name: "Review Plan"
  short_description: "Blind-review an implementation plan before execution"
  default_prompt: "Use $review-plan to independently review this implementation plan before execution: $ARGUMENTS"
